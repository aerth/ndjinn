{{define "title"}}{{.GlobalSiteName}}{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
	<div class="page-header">
		<h1>{{template "title" .}}</h1>
	</div>
	<p>Click {{LINK "login" "here"}} to login.</p>
	<p>Click {{LINK "register" "here"}} to become a member.</p>
	{{template "footer" .}}
</div>
{{end}}
{{define "foot"}}{{end}}
